const initialState = {
    tasks: []
};

const tasksReducer = (state = initialState, action) => {
    switch (action.type) {
        // Define your cases here
        default:
            return state;
    }
};

export default tasksReducer;
